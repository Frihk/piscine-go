func AppendRange(min, max int) []int {
	slic := []int{}
	if min > max {
		return nil
	}
	for i := min; i < max; i++ {
		slic = append(slic, i)
	}
	return slic
}

func main() {
	fmt.Println(AppendRange(5, 10))
	fmt.Println(AppendRange(10, 5))
}
